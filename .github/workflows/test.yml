name: Testare Cod

on: [push]

jobs:
  ruleaza-scriptul:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Configureaza Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - name: Ruleaza codul meu
        run: python salut.py
